{
  "name": "@stillflow/dev-fixtures",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "//authority": [
    "Target topology contract (FE0-C2 §1): data factories for tests / Storybook / dev entries ONLY.",
    "Forbidden edges: contracts / client / studio-ui must never depend on this package, and",
    "production app entries must never import it — enforced by scripts/check-dependency-boundaries.mjs.",
    "Migration of existing demo data lands in FE1-S5 (openship#23)."
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo node_modules"
  },
  "dependencies": {
    "@stillflow/contracts": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
